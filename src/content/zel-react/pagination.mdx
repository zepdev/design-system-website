---
label: Elements
title: Pagination
---

import Table from '@zlab-de/zel-react/Table'
import TableHeader from '@zlab-de/zel-react/TableHeader'
import TableCell from '@zlab-de/zel-react/TableCell'
import TableBody from '@zlab-de/zel-react/TableBody'
import TableRow from '@zlab-de/zel-react/TableRow'
import Pagination from '@zlab-de/zel-react/Pagination'
import PaginationExample from '../../components/pagination/PaginationExample'

# Pagination

The Zeppelin Pagination element requires the use of javascript.

---

## Available Props

<Table title="Pagination Props" style={{ marginBottom: 60 }}>
  <TableHeader>
    <TableCell>Prop</TableCell>
    <TableCell>Type</TableCell>
    <TableCell>Default</TableCell>
    <TableCell>Required</TableCell>
  </TableHeader>
  <TableBody>
    <TableRow color="white">
      <TableCell scope="row" component="th">
        className
      </TableCell>
      <TableCell>string, object</TableCell>
      <TableCell />
      <TableCell />
    </TableRow>
    <TableRow color="gray">
      <TableCell scope="row" component="th">
        pages (total num of pages)
      </TableCell>
      <TableCell>func</TableCell>
      <TableCell />
      <TableCell>Yes</TableCell>
    </TableRow>
    <TableRow color="white">
      <TableCell scope="row" component="th">
        pagesToDisplay (num of page buttons to display)
      </TableCell>
      <TableCell>number, string</TableCell>
      <TableCell />
      <TableCell>Yes</TableCell>
    </TableRow>
    <TableRow color="gray">
      <TableCell scope="row" component="th">
        currentPage
      </TableCell>
      <TableCell>number, string</TableCell>
      <TableCell />
      <TableCell>Yes</TableCell>
    </TableRow>
    <TableRow color="white">
      <TableCell scope="row" component="th">
        onPageChange
      </TableCell>
      <TableCell>func</TableCell>
      <TableCell />
      <TableCell>Yes</TableCell>
    </TableRow>
  </TableBody>
</Table>

## Example

<div style={{ marginBottom: 30 }}>
  <PaginationExample />
</div>

```
import React, {useState} from 'react'
import Pagination from '@zlab-de/zel-react/Pagination'

function MyComponent(){
    const [count, setCount] = useState(0)

    return(
        <div>
            <Pagination
                label="demo number input"
                count={count}
                onChange={value => setCount(value)}
                onMinus={() => setCount(count - 1)}
                onPlus={() => setCount(count + 1)}
            />
        </div>
    )
}

export default MyComponent
```
