---
label: inputCodeExample_Vue
title: Code
---

```html
<!--
    ------------------------------------
    vue.js Single File Component example
    ------------------------------------

    use as:

    <ZeppelinInput placeholder="Placeholder" id="input-zeppelin" label="My input field"/>
-->
<template>
  <div class="zep-form-container">
    <input
      type="text"
      class="zep-input"
      v-bind:id="id"
      v-bind:placeholder="placeholder"
    />
    <label class="zep-form-container__label" v-bind:for="id">{{ label }}</label>
  </div>
</template>

<script>
  export default {
    name: 'ZeppelinInput',
    data: function() {
      return {
        count: 0,
      }
    },
    props: {
      placeholder: {
        type: String,
        required: true,
      },
      id: {
        type: String,
        required: true,
        default: 'input-zeppelin',
      },
      label: {
        type: String,
        required: true,
      },
    },
  }
</script>
```
