---
label: tagCodeExample_React
title: Code
---

```jsx
import React from 'react'
import PropTypes from 'prop-types'
import IconClose from '@zlab-de/zel-react-icons/IconClose'

function Tag({ children, onClick }) {
  return (
    <div>
      <div role="button" className="zep-tag">
        {children}
        <button
          className="zep-tag__button"
          aria-label="close"
          onClick={onClick}
        >
          <IconClose className="zep-tag__icon" />
        </button>
      </div>
    </div>
  )
}

Tag.propTypes = {
  onClick: PropTypes.func,
  children: PropTypes.string,
}

export default Tag
```
