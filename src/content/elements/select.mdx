---
label: Elements
title: Select
---

import ElementLayout from '../../components/elements/ElementLayout.jsx'

# Select

Select is a type of input users use to select one item from a list of values. In some cases the user can select multiple attributes. The selected item is always visible when the select box is closed.

---

<ElementLayout element="select" />
